#include <iostream>
using namespace std;

int main() {
    int k;
    cin >> k;

    int h = k / 3600;        // Вычисляем целые часы
    int m = (k % 3600) / 60; // Вычисляем целые минуты

    cout << "It is " << h << " hours " << m << " minutes." << endl; // Выводим результат

    return 0;
}
