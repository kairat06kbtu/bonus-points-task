
#include <iostream>

int main() {
    int x, d, count = 0;
    std::cin >> x >> d;

    while (x > 0) {
        if (x % 10 == d) {
            count++;
        }
        x /= 10;
    }

    std::cout << count << std::endl;
    return 0;
}
